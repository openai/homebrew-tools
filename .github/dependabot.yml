version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    # Let ordinary releases age eight days; security updates bypass cooldown.
    cooldown:
      default-days: 8
    # .github/CODEOWNERS routes workflow reviews to @openai/sdks-team.
